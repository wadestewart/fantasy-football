<h1>Players</h1>
<%= react_component("HelloWorld", props: @hello_world_props, prerender: false) %>

<%= @players.each do |player| %>
    <div class="player">
        <h2><%= link_to "#{player.name} - #{player.position} for #{player.nfl_team}"%></h2>
    </div>
<% end %>
